# --- Phase 1: audio / beat detection ---
numpy
librosa
soundfile          # librosa I/O backend

# --- Phase 2: kill detection ---
opencv-python

# --- Config / CLI ---
pyyaml

# --- Optional: pull audio from a link for testing ---
yt-dlp

# NOTE: FFmpeg/ffprobe are external binaries (installed via winget), not pip packages.
# Target interpreter: Python 3.12 (numba/librosa/opencv lack 3.14 wheels).
