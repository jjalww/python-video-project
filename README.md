# python-video-project

A Python video project.

## Setup

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# macOS/Linux
source venv/bin/activate

pip install -r requirements.txt
```

> Note: `venv/`, `__pycache__/`, and video/audio media files are git-ignored.
